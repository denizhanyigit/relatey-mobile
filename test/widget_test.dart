import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Relatey app smoke test', (WidgetTester tester) async {
    // Build our app and trigger a frame.
    // Note: Full widget testing requires mocking providers
    // This is a placeholder test
    expect(1 + 1, equals(2));
  });
}
